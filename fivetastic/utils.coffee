utils = {}
# utils.something = ->
  
  
  
# function compile_coffee_global(string) {
#   coffee = CoffeeScript.compile(string)
#    lines = coffee.split("\n")
#    lines = _(lines).reject(function(line){
#      idx = _(lines).indexOf(line)
#      return idx == 0 || idx == lines.length-2
#    })
#    lines = lines.join("\n")
#    eval(lines)
# }